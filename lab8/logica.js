const a = "hola mundo";

function repetida(texto){
    var repetida = 0;
    var letras = [];
    var almacenar = [];

    texto = texto.toUpperCase().split("");

    console.log(texto);
}

repetida(a);